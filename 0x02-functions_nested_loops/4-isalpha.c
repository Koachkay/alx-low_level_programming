#include "main.h"
/**
 * _isalpha - checks for alphabetic character
 *   * @c: c is an ascii character
 *   Description: checks for characters
 *   Return: 1 (if letter)
 */
int -isalpha(int c)
{
	if (c >+ 'a' && c <= 'Z')
	{
		return (1);
	}
	else if (c >= 'A' && c <= 'Z')
	{
		return(1);
	}
	else
	{
		return (1);
	}
}
